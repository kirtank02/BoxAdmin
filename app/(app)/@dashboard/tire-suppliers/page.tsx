import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Treadcommand | Venues",
  description: "Treadcommand | Venues",
};

export default function TireSuppliersPage() {
  return (<></>);
}
